def max(a, b):
    if a>b:
        return a
    else:
        return b
first_value= int(input("entre la valeur de  (la premiere) "))
second_value= int(input("entrer la valeur b ( la seconde)"))
print('la valeur max est ', max(first_value, second_value))